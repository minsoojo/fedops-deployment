FedOps chart rendered for namespace {{ .Release.Namespace }}.
Web: {{ include "fedops.publicUrl" (dict "root" . "host" "webHost") }}/fedops

Before installation: prepare verified app images, F-only Secrets, local directories,
MinIO buckets/app credentials, and the separate Istio/MetalLB installation.
The ingress Service must expose {{ ternary 443 80 (eq .Values.access.scheme "https") }} and every TCP port {{ .Values.task.portMin }}-{{ .Values.task.portMax }}.
Manager owns {{ .Values.istio.taskVirtualServiceName }} and all dynamic Tasks.
Keep FL range, Gateway name, and host stable while Tasks are active.
PV/PVC are retained on uninstall; use persistence.*.existingClaim to reconnect.
Rendering/lint success does not confirm application readiness or Task/E2E success.
